# Koken

`koken.md` is de waarheid: calorieën, macro's en wat hij niet lust.

`weekmenu.html` is het weekmenu, gebouwd uit `menu/<week>.json` met `python3 build.py`. Eén document per week dat hij één keer opent, geen dashboard.

Wanneer hij kookt en boodschappen doet staat in `Context/schema.md`.
