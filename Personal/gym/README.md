# Gym

`gym.md` is de waarheid. `schema/` bevat de uitgewerkte oefenschema's als HTML, `data/strong/` de export uit de Strong-app en `data/metingen/` de oude metingen.

Wanneer hij traint staat in `Context/schema.md`. Wat hij eet in `Personal/koken/`.
